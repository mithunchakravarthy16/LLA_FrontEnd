import { createTheme } from "@mui/material/styles";
import theme from "./theme";

// export default createTheme({
//     palette: {
//       defaultTheme: theme.defaultTheme.palette,
//       greenTheme: theme.greenTheme.palette,
//       redTheme: theme.redTheme.palette,
//       yellowTheme: theme.yellowTheme.palette
//     },
// });